# Optional local-development overrides.
# If these are omitted, the web app uses same-origin defaults:
# REST -> /api
# WebSocket -> ws(s)://current-host/ws
VITE_API_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws
